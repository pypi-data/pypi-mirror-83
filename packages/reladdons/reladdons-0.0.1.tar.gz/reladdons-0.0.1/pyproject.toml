[tool.poetry]
name = "reladdons"
version = "0.0.1"
description = "the first version"
authors = ["D1ffic00lt's Community <dm.filinov@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
