from setuptools import setup

setup(name='fcc_distributions',
      version='0.1',
      description='Gaussian  and binomial distributions',
      packages=['fcc_distributions'],
      author='Francisco Chavez',
      author_email='something@else.com',
      zip_safe=False)
