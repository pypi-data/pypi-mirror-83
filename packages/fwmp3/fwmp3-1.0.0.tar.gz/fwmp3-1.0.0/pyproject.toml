[tool.poetry]
name = "fwmp3"
version = "1.0.0"
description = "python 3.8"
authors = ["John Smith <azarovnick@bk.ru>"]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
gtts = "pyAesCrypt"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
