from setuptools import setup

setup(name='distribs_probability',
      version='0.1.1',
      description='All probability distributions',
      packages=['distribs_probability'],
      zip_safe=False)
