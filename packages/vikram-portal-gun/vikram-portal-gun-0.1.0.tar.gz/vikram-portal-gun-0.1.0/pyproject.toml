[tool.poetry]
name = "vikram-portal-gun"
version = "0.1.0"
description = ""
authors = ["Vikash_Vikram <Vikash_Vikram@external.mckinsey.com>"]
readme = "README.md"

[tool.poetry.scripts]
rick-portal-gun = "vikram_portal_gun.main:app"

[tool.poetry.dependencies]
python = "^3.9"
typer = {extras = ["all"], version = "^0.3.2"}

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
