from setuptools import setup

setup(name='niti_distributions',
      version='0.1',
      description='Gaussian & Binomial distributions',
      packages=['niti_distributions'],
      zip_safe=False)
