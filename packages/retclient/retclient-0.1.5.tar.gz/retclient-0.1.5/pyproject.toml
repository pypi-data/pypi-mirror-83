[tool.poetry]
name = "retclient"
version = "0.1.5"
description = "A client package for RetinaFace."
authors = ["reeve0930 <reeve0930@gmail.com>"]
license = "MIT"
readme = "README.md"
repository = "https://github.com/reeve0930/pyzaim"
keywords = ["face detection", "Retina Face"]

[tool.poetry.dependencies]
python = "^3.7"
opencv-python = "^4.4.0"
numpy = "^1.19.2"
requests = "^2.24.0"
reelib = "^1.4.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
