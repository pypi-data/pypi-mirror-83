[tool.poetry]
name = "crostab"
version = "0.0.6"
description = "cross table analytical tool"
authors = ["hazen <hoyeungw@outlook.com>"]

[tool.poetry.dependencies]
python = "^3.8"
veho = ">=0.0.4"
aryth = ">=0.0.6"
ject = ">=0.0.2"

[tool.poetry.dev-dependencies]
pytest = ">=6.0.0"
pyspare = ">=0.0.1"
intype = ">=0.0.1"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
