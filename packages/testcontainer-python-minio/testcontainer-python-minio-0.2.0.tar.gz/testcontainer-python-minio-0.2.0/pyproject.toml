[tool.poetry]
name = "testcontainer-python-minio"
version = "0.2.0"
description = ""
authors = ["Max Fröhlich <max.froehlich@serviceware.de>"]

[tool.poetry.dependencies]
python = "^3.6"
testcontainers = { version = "^3.0" }

[tool.poetry.dev-dependencies]
pytest = "^5.3.4"
flake8 = "^3.7.9"
black = "^19.10b0"
boto3 = "^1.13.1"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
